/*
	P5Q8: Ask integer from user and show all the all divisors in ascending order
	which loop to use? while or for

*/

#include <iostream>
using namespace std;
int main()
{
	// declare variable
	int num;

	// get input
	cout << "Enter a number : ";
	cin >> num;

	// if statement to make sure the input is positive
	if (num > 0)
	{
		// ?? use for loop the show all divisor based on input
		cout << "The divisors are : ";
		


	}
	else
		cout << "You have entered a zero or negative number." << endl;
	return 0;
}
